let selectedColors = ['red', 'blue'];
selectedColors[2] = 1;
// the arrays are dynamic also the object of the array ans the size of the array

//console.log(typeof selectedColors);
//console.log(selectedColors.length);
console.log(selectedColors);