// if a customer has more than 100 points in the
//they are a 'glod' customer, otherwise,
//they are a 'silver' customer.

let points = 110;
let type = points > 100 ? 'gold' : 'silver';
//ternary or conditinel operator
console.log(type);